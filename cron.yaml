cron:
- description: daily crawl job
  url: /page/admin/parse/start
  schedule: every 48 hours

cron:
- description: persist scoring info of channel list to datastore
  url: /cron/persistchannel
  schedule: every 24 minutes
